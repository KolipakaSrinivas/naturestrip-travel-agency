// next.config.js
const nextConfig = {
  output: 'export',
  // Optional: if using images, add this to support image optimization
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
